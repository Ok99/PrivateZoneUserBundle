{# @var \Ok99\PrivateZoneCore\UserBundle\Entity\User user #}
{{ 'resetting.email.message'|trans({
    '%name%': user.name,
    '%regnum%': user.fullRegnum,
    '%confirmationUrl%': 'odkaz: '~confirmationUrl,
}, 'FOSUserBundle') }}
